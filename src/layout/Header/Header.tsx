import { SearchButton, SearchInput, Box } from "./headerStyles";

const Header = () => {
  return (
    <Box>
      {/* <SearchInput placeholder="Search products..." variant="outlined" />
      <SearchButton variant="contained">Search</SearchButton> */}
    </Box>
  );
};

export default Header;
